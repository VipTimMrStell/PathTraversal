version: '3'
services:
  vulnerable-app:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./app:/app
    command: sh -c "pip install flask -q && python server.py"
    ports:
      - "5000:5000"
